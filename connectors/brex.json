{
  "id": "brex",
  "name": "Brex",
  "category": "Spend Management",
  "description": "Brex integration for automated workflows",
  "version": "1.0.0",
  "authentication": {
    "type": "oauth2",
    "config": {
      "authUrl": "https://accounts.brex.com/oauth/authorize",
      "tokenUrl": "https://accounts.brex.com/oauth/token",
      "scopes": [
        "transactions:read",
        "users:read"
      ]
    }
  },
  "actions": [
    {
      "id": "create_record",
      "name": "Create Record",
      "description": "Create a new record in Brex",
      "parameters": {
        "data": {
          "type": "object",
          "required": true,
          "description": "Record data"
        }
      },
      "requiredScopes": [
        "transactions:read",
        "users:read"
      ]
    },
    {
      "id": "update_record",
      "name": "Update Record",
      "description": "Update an existing record in Brex",
      "parameters": {
        "id": {
          "type": "string",
          "required": true,
          "description": "Record ID"
        },
        "data": {
          "type": "object",
          "required": true,
          "description": "Updated record data"
        }
      },
      "requiredScopes": [
        "transactions:read",
        "users:read"
      ]
    },
    {
      "id": "get_record",
      "name": "Get Record",
      "description": "Get a record from Brex",
      "parameters": {
        "id": {
          "type": "string",
          "required": true,
          "description": "Record ID"
        }
      },
      "requiredScopes": [
        "transactions:read",
        "users:read"
      ]
    }
  ],
  "triggers": [
    {
      "id": "record_created",
      "name": "Record Created",
      "description": "Triggered when a new record is created",
      "parameters": {},
      "webhook": {
        "eventTypes": [
          "created"
        ],
        "entityTypes": [
          "record"
        ]
      },
      "requiredScopes": [
        "transactions:read",
        "users:read"
      ]
    },
    {
      "id": "record_updated",
      "name": "Record Updated",
      "description": "Triggered when a record is updated",
      "parameters": {},
      "webhook": {
        "eventTypes": [
          "updated"
        ],
        "entityTypes": [
          "record"
        ]
      },
      "requiredScopes": [
        "transactions:read",
        "users:read"
      ]
    }
  ]
}